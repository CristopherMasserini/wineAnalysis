from gensim.models import Word2Vec
import gensim


def word2Vec_model(data):
    """
    Create a word2Vec model for the descriptions and the aimed for column.

    example: model = gensim.models.Word2Vec(data, min_count=1, vector_size=100, window=5)
    :param data:
    :return:
    """
    pass


def bagOfWords_model(data):
    """
    Create a bag of words model for the grouped descriptions and aimed for column.

    :param data:
    :return:
    """

    pass

